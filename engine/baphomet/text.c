#include <baphomet/text.h>

FT_Library ft;
/*
text_initialize() {
  if (FT_Init(&ft)) {
    fprintf(stderr, "Unable to initialize freeType\n");
    return 1;
  }
}

text_render(char* text) {
  char* font = "/usr/share/fonts/TTF/Marcellus-Regular.ttf"; // Also try Vera.ttf
  FT_Face face;

  if (FT_New_Face(ft, font, 0, &face))

  FT_Set_Pixel_Sizes(face, 0, 48);

  // Is updated after a glyph/character is loaded by FT_Load_Char(face, 'C', FT_LOAD_RENDER)
  FT_GlyphSlot g = face->glyph;
}
*/
